<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DCMAUI.Views.Attendance.AttendancePage"
             Title="Attendance">
    <CollectionView x:Name="AttendanceCollection" Margin="16">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="0,6" CornerRadius="10" Padding="16">
                    <Grid ColumnDefinitions="*,Auto,Auto">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="{Binding Date, StringFormat='{0:MMM dd, yyyy}'}" FontAttributes="Bold"/>
                            <Label Text="{Binding NetTime}" TextColor="Gray" FontSize="12"/>
                        </VerticalStackLayout>
                        <Label Grid.Column="1" Text="{Binding Status}" TextColor="#43C59E" VerticalOptions="Center" Margin="8,0"/>
                        <Label Grid.Column="2" Text="{Binding Earnings, StringFormat='₹{0:N0}'}" FontAttributes="Bold" VerticalOptions="Center"/>
                    </Grid>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
